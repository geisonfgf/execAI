"""
Setup script for execAI.

This file provides backward compatibility with older pip versions
that don't support pyproject.toml fully.
"""

from setuptools import setup

if __name__ == "__main__":
    setup() 